# PostgreSQL Database Connection URL (e.g. Supabase, Neon, or local Postgres)
# Defaults to SQLite local database if empty or Postgres not available
DATABASE_URL=sqlite:///./sinergia.db

# API Settings
HOST=0.0.0.0
PORT=8000
ENV=development
